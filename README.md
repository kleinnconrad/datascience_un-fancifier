# datascience_un-fancifier
Now that the Date Science supertrend era of predicting everything with logistic regression is over it's time for some UNfancifying of logistic regression functions. This repo provides a CLI tool to transform your logistic regression function into if-then-else.
